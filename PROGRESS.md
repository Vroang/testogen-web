# PROGRESS — веб-версия

## Текущий шаг: 3 — банк вопросов (список)
## Статус: шаг 3 выполнен

## Что сделано (шаг 3)
- Экран «Банк вопросов» (src/pages/QuestionsPage.tsx) вместо заглушки:
  загрузка всех вопросов пользователя из Supabase (select * where
  user_id, сортировка по created_at по убыванию).
- Общая шапка (src/components/AppHeader.tsx): логотип «Т», заголовок,
  имя пользователя, «Выйти».
- Под шапкой: «← На главную» и счётчик «Всего: N вопросов».
- Фильтр по темам — чипсы: «Все темы» (по умолчанию), уникальные темы
  (по алфавиту), «Без темы» (если есть вопросы без topic). Активный —
  teal-заливка, неактивный — белый с рамкой. Фильтрация на клиенте.
- Карточки вопросов: свёрнуто — текст до 2 строк с многоточием и
  метки-чипсы (сложность: Лёгкий/Средний/Сложный — зелёный/янтарный/
  красный; «С подвохом»; источник: Из файла / Интернет/ИИ / Вручную;
  тема). Развёрнуто — полный текст, варианты «А)…»–«Г)…», правильный
  подсвечен зелёным с галочкой, остальные серые; иконки «карандаш» и
  «корзина». Раскрытие плавное (~200 мс), несколько карточек можно
  раскрыть одновременно.
- Удаление: диалог «Удалить вопрос? Он удалится из мобильного
  приложения тоже.» с «Отмена» / красной «Удалить»; при подтверждении
  delete в Supabase, удаление из state, toast «Вопрос удалён».
- Карандаш — toast «Редактирование — в следующем шаге» (форма в шаге 4).
- Состояния: загрузка («Загружаем вопросы…»), ошибка (красный текст +
  кнопка «Повторить»), пусто («Пока нет вопросов…»), фильтр пуст
  («В этой теме нет вопросов.»).
- Адаптивность: 2 колонки от md, на узких — одна. Стиль: #FAFAF7,
  акцент #0E7C6B, скругления 16px, мягкие тени.
- Проверено в браузере с тестовыми данными (временный перехватчик
  fetch, код не менялся): список 6 вопросов, чипсы всех тем, фильтры
  (История России → 3, Без темы → 1, Все темы → 6), раскрытие/сворачивание,
  правильный ответ «А) 988 ✓» зелёным, диалог удаления, удаление
  (6 → 5, счётчик обновился, тост), тост карандаша, «← На главную».
  Сборка без ошибок. Тестовая сессия и данные убраны.
- Отправлено в GitHub (main).

## Что сделано (шаг 2)
- Роутинг: /login, /home, /questions, /textbooks, /settings.
- Главный экран: шапка, приветствие, три карточки со счётчиками
  (questions/textbooks, «…» при загрузке, «—» при ошибке), «Выйти»,
  нижний блок про синхронизацию. Адаптив 3 колонки / 1 колонка.

## Что сделано (шаг 1)
- Экран входа: Email + Пароль, signInWithPassword, красные ошибки
  по-русски, редиректы, сессия сохраняется (localStorage).

## Что сделано (шаг 0)
- Установлены Node.js v24.21.0 LTS и Git 2.55.0; проект Vite + React 18
  + TypeScript; зависимости react-router-dom, @supabase/supabase-js,
  tailwindcss v3; VISION.md, PLAN.md, PROGRESS.md; Supabase-клиент;
  экран-заглушка; публикация в GitHub.

## Известные проблемы
- Проверка ключа Supabase через curl давала «Invalid API key», однако
  пользователь подтвердил, что ключ валиден; реальный запрос из
  браузера проходит (сервер отвечает ошибкой авторизации при неверном
  пароле — ключ принят). Игнорируем.
- Реальные данные банка вопросов проверяются при входе настоящим
  аккаунтом; с тестовыми данными весь интерфейс проверен.

## Заметки
- Инструменты установлены в пользовательские папки:
  Node — %LOCALAPPDATA%\Programs\nodejs,
  Git (Portable) — %LOCALAPPDATA%\Programs\Git.
- Прокси для GitHub настроен в %USERPROFILE%\.gitconfig
  (http://127.0.0.1:10808).
- В системном конфиге PortableGit заменён credential-helper-selector
  на store (селектор показывал невидимый диалог и puши зависали);
  токен GitHub — в %USERPROFILE%\.git-credentials.
- Tailwind v3 (tailwind.config.js + @tailwind). Сессия Supabase —
  в localStorage (persistSession включён по умолчанию).
- Тестовые сессии/данные подкладывались только временно в браузере
  и удалены после проверки; в коде их нет.
