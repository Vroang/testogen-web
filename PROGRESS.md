# PROGRESS — веб-версия

## Текущий шаг: 11 — управление вопросами в черновике + компактные ответы
## Статус: шаг 11 выполнен

## Что сделано (шаг 11)
- Черновик /draft: у каждого вопроса в развёрнутом варианте — полоса
  с тремя иконками (Заменить 🔄 слева, Редактировать ✏️ по центру,
  Убрать 🗑️ справа; серые, при наведении teal/красный).
- Редактировать → /questions/:id/edit?returnTo=draft; после
  сохранения вопрос обновляется в черновике: при монтировании /draft
  данные всех вопросов черновика подтягиваются из Supabase по id
  (пачками по 100) и подменяются в черновике.
- Убрать → диалог «Убрать вопрос из этого черновика? В банке
  вопросов он останется.» (Отмена / красная «Убрать»); удаляется
  только текущая копия из варианта (не из банка и не из других
  вариантов); тост «Вопрос убран из черновика»; пустой вариант —
  серый текст «В этом варианте нет вопросов».
- Заменить → диалог «Замена вопроса»: превью заменяемого вопроса,
  поле «Причина замены» с чипсами-подсказками (Не по теме,
  Не проходят в этом классе, Слишком сложный, Слишком лёгкий,
  Неоднозначный, Другое); пустая причина → тост «Укажите причину
  замены»; поиск замены в банке: та же тема (без регистра), другой
  id, не входит в текущий вариант, сложность из разрешённых теста
  (если известны). Найден — замена в черновике + тост «Вопрос
  заменён» + причина пишется в replacement_reasons; не найден —
  диалог «В банке нет подходящих вопросов. Что делать?»
  (Сгенерировать через ИИ с темой заменяемого / Вписать вручную /
  Отмена).
- Экспорт ответов компактнее: шрифт таблиц 11pt, вертикальные отступы
  ячеек 2pt, «Вариант N» 14pt с минимальными отступами, без разрывов
  страниц (Word переносит сам).
- GeneratePage: предзаполнение темы из sessionStorage при переходе
  «Сгенерировать через ИИ» из диалога замены.
- Проверено в браузере с мок-банком: панель иконок; удаление копии
  (тост, вариант уменьшился, второй вариант не тронут); пустая
  причина → тост; чипс подставляет причину. Полное обновление из
  Supabase требует реальной сессии — механика написана. Сборка без
  ошибок, тестовые данные убраны.
- Отправлено в GitHub (main).

## Что сделано (шаг 10.1)
- Документ «Тест»: разрыв страницы перед вариантами 2+ только
  (вариант 1 идёт сразу после заголовка — пустого первого листа
  больше нет); «Вариант N» — отступ после 8pt; между вопросами —
  6pt; между вариантами ответа — 0pt; межстрочный интервал 1.15;
  убраны пустые абзацы после вопросов. Вариант из 10 вопросов
  помещается на один лист.
- Документ «Ответы»: убран лишний отступ в начале — заголовок и
  первая таблица на одной странице; «Вариант N» компактнее
  (before 6pt / after 3pt).
- Проверено в браузере по структуре XML обоих файлов: в «Тесте»
  ровно 1 разрыв страницы, отступы after=160 (заголовок варианта)
  и before=120 (вопросы), интервал 276 twips (1.15), пустых
  абзацев нет; в «Ответах» разрывов нет. Сборка без ошибок,
  тестовые данные убраны.
- Отправлено в GitHub (main).

## Что сделано (шаг 10)
- Экспорт черновика в Word (docx + file-saver): «Тест» (заголовок,
  варианты с вопросами А–Г, каждый вариант с новой страницы) и
  «Ответы» (таблицы №/Ответ); имена файлов Title Case с очисткой
  символов, пустая тема → «ТестоГен»; кнопки на /draft с
  состояниями «Готовим файл…»/«Файл скачан». Исправлен баг залипания
  busy-состояния кнопок.

## Что сделано (шаг 9)
- Четвёртая карточка «Собрать тест» (📝) на главной → /build.
- Форма сборки /build: тема теста с автодополнением по темам банка;
  дополнительные темы (модальный список с чекбоксами, выбранные —
  чипсами); слайдеры «Количество вариантов» (1-40) и «Вопросов в
  каждом варианте» (5-40) с полями ввода; чипсы сложности
  (мультивыбор); чекбоксы «Включать вопросы с подвохом» и
  «Показывать правильные ответы в конце отдельным блоком»;
  инфо-плашка «Будет собрано: X вариантов × Y вопросов = Z нужно»
  (красная «В банке только N подходящих вопросов» при нехватке);
  кнопка «✨ Собрать тест».
- Логика: пул = главная тема (если задана) + фильтры сложности/
  подвоха; добор из дополнительных тем при нехватке; если 0
  подходящих — диалог «По теме „X“ нет вопросов. Сгенерировать их?»
  (Отмена / Сгенерировать → /questions/generate); если меньше
  нужного — диалог с 3 кнопками (Отмена / Сгенерировать через ИИ /
  Собрать с повторами). Алгоритм: перемешанный пул, вариант i берёт
  вопросы с позиции i*Q по кругу, порядок внутри варианта
  перемешан.
- Черновик /draft: данные в sessionStorage; без черновика — редирект
  на /build; счётчик «Вариантов: N · Вопросов в каждом: M»;
  аккордеоны «Вариант N» (свёрнуты, тап — раскрытие) с вопросами
  «1. Текст» и вариантами А–Г (правильный — зелёный с галочкой);
  кнопки «📥 Скачать тест (.docx)» / «📥 Скачать ответы (.docx)» →
  тост «Экспорт — в следующем шаге» (Шаг 10).
- Параметры формы сохраняются в sessionStorage — «← Изменить
  параметры» возвращает к заполненной форме.
- Проверено в браузере с мок-банком (16 вопросов, темы «Древний
  Египет»/«Средние века», tricky, без темы): 4 карточки на главной;
  автодополнение; красная плашка при нехватке (6 из 10; с доп.
  темами 9 из 10); диалог нехватки с 3 кнопками → «Собрать с
  повторами» → черновик 2×5, варианты различаются, правильный
  зелёный; возврат с сохранёнными параметрами; диалог «По теме…
  нет вопросов» → редирект на генерацию; /draft без черновика →
  /build; тосты экспорта. Сборка без ошибок, тестовые данные
  убраны.
- Отправлено в GitHub (main).

## Что сделано (шаг 8.1)
- Фикс резерва каскада: динамический резерв (первые 3 бесплатные
  из актуального каталога, 'openrouter_fallback_models'), дефолтный
  резерв из актуальных бесплатных моделей; ошибки «unavailable for
  free»/404/410 пропускаются молча; итоговый тост — ошибка
  последней модели + подсказка; плашка для устаревшей Llama в
  настройках.

## Что сделано (шаг 8)
- Генерация вопросов из параграфов учебника: рефакторинг openrouter
  (общее ядро каскада requestQuestionsFromAI + общий цикл
  generateQuestionsBatch: порции по 10, дубли, insert, прогресс);
  кнопка «✨ Сгенерировать вопросы» на /textbooks/:id; маршрут
  /textbooks/:id/generate (тема с автотемой, «По параграфам» /
  «По страницам» (PDF), быстрый диапазон + список галочек,
  количество 1-50, мультивыбор сложностей); текст параграфов через
  «---», обрезка 8000 символов; системный промпт «ТОЛЬКО текст
  учебника»; сохранение с source='pdf'.
- Кнопка «✨ Сгенерировать вопросы» на /textbooks/:id.
- Новый маршрут /textbooks/:id/generate (TextbookGeneratePage):
  тема вопросов (пусто → автотема «имя учебника § X–Y» или
  «§ 4, 5, 7» для непоследовательных), переключатель «По
  параграфам» / «По страницам» (страницы только для PDF, иначе
  тост «Выбор по страницам доступен только для PDF»), быстрый
  диапазон «От § / До § + Выделить», список параграфов с
  галочками («§ N без названия» если title пуст, страницы для
  PDF), режим «По страницам» с полями «От стр. / До стр.» и
  превью «Будут включены § 2–3 (страницы 2–3)» либо «В этом
  диапазоне нет параграфов»; параметры: количество 1-50 (по
  умолчанию 10) и мультивыбор сложностей; «Отмена» и «✨
  Сгенерировать» (неактивна без выбора, при работе — «Генерируем…
  N из M»).
- Логика: текст отмеченных параграфов через «\n\n---\n\n»,
  обрезка до 8000 символов; в режиме страниц § включается целиком,
  если его диапазон страниц пересекается с выбранным; системный
  промпт дополнен «Используй ТОЛЬКО текст учебника…»; сохранение с
  source = 'pdf'; дубли отсеиваются; успех — «Добавлено N вопросов»
  → редирект на /questions; без ключа → тост и редирект в
  /settings; ошибки — красные тосты.
- Проверено в браузере с моками (код не менялся): форма (тема
  пустая, «По параграфам» активен, 0 галочек, кнопка неактивна,
  количество 10, все сложности); «Выделить» 1-3 → 3 галочки;
  генерация — промпт содержит базовые правила + дополнение «ТОЛЬКО
  текст учебника», разделитель «---», тему; вопросы в списке с
  метками «Лёгкий», «Из файла», «Учебник, § 1-3»; TXT + «По
  страницам» → тост; PDF + страницы 2-3 → превью «Будут включены
  § 2–3 (страницы 2–3)» и «Добавлено 1 вопрос» с автотемой
  «rome-pages § 2–3». Сборка без ошибок, тестовые данные убраны.
- Отправлено в GitHub (main).

## Что сделано (шаг 7)
- Подключены библиотеки pdfjs-dist 6.3.289 (PDF) и mammoth 1.12.3
  (DOCX) — единственные новые зависимости, разрешены заданием.
- Парсер учебников (src/lib/textbookParser.ts): извлечение текста
  постранично из PDF (pdf.js, worker через Vite ?url), DOCX через
  mammoth.extractRawText, TXT через FileReader; разбивка по разметке
  «§ N» (number, title — строка после § до переноса, до 100 символов,
  text, start_page — страница, где начинается §, end_page — страница
  перед следующим §; для DOCX/TXT всё = 1). Текст до первого §
  отбрасывается. Если § не найдены — понятная ошибка, загрузка
  не начинается.
- Экран /textbooks (вместо заглушки): шапка, «← На главную»,
  счётчик «Всего: N учебников», кнопка «+ Загрузить учебник»;
  карточки (название, «PDF · 25 параграфов · загружен 19 сентября
  2026», корзина), клик по карточке → /textbooks/:id; пустое
  состояние; загрузка/ошибка/«Повторить»; сортировка по uploaded_at
  desc.
- Загрузка: скрытый input (accept .pdf/.docx/.txt); проверка формата
  (тост «Неподдерживаемый формат»); размер >50 МБ → диалог «Файл
  слишком большой (X МБ)…»; индикатор «Обрабатываем файл…» с этапами
  и процентами (чтение 0-30, извлечение 30-60, разбивка 60-70,
  облако 70-100); UUID через crypto.randomUUID(); файл в Storage
  textbooks по пути userId/textbookId.ext (upsert); insert в
  textbooks; вставка параграфов пачками по 100; при ошибке вставки —
  откат (файл из Storage + запись учебника) и красный тост; успех —
  «Учебник загружен: N параграфов» и обновление списка.
- Экран /textbooks/:id: шапка «← Мои учебники» + название; метаданные
  + красная кнопка «Удалить учебник»; список параграфов «§ 4.
  Реформация», для PDF «стр. X–Y», превью 100 символов, тап — полный
  текст; удаление с диалогом «Все параграфы и файл будут удалены.
  Отменить нельзя.» → параграфы + учебник + Storage-файл → редирект.
- Удаление доступно и корзиной на карточке списка (тот же диалог).
- Проверено в браузере с моками REST и Storage (код не менялся):
  TXT e2e (3 параграфа, заголовки, страницы 1-1, текст до §
  отброшен); hand-crafted 3-страничный PDF → 3 параграфа со
  страницами 1/1, 2/2, 3/3 и превью; просмотр и раскрытие;
  удаление из просмотра и корзиной из списка (Storage + обе таблицы
  очищены); ошибка вставки параграфов → откат; формат .rtf → тост;
  файл 51 МБ → диалог про размер. Сборка без ошибок, тестовые данные
  убраны.
- Отправлено в GitHub (main).

## Что сделано (шаг 6.1)
- Динамический список моделей из OpenRouter (публичный /models),
  правило бесплатности (':free' или нулевые цены), сортировка
  (бесплатные по имени → платные по цене input), кэш на 1 час в
  localStorage, фильтр «Только бесплатные» (по умолчанию) / «Все
  модели», выпадающий список с ценами за 1M токенов и галочкой,
  auto закреплён первым, «Обновить список» (force) и «Повторить»
  при ошибке. Генерация не менялась.
- UI на /settings: фильтр-чипсы «Только бесплатные» (по умолчанию) /
  «Все модели»; выпадающий список — auto закреплён первым (даже если
  его нет в API), каждая строка: name + id серым + справа метка
  «Бесплатно» (зелёная) или «$X.XX / $Y.YY за 1M токенов» (серая);
  выбранная модель — teal-галочка; выбор сразу сохраняется в
  'openrouter_model'; при открытии выбор восстанавливается. Пока
  грузится — «Загружаем список моделей…»; при ошибке — красный текст
  и «Повторить». Кнопка «Проверить ключ» работает как раньше.
- Генерация (шаг 6) не менялась: модель берётся из
  'openrouter_model', каскад — основная → фиксированный резерв.
- Проверено в браузере с моком списка моделей: только бесплатные —
  auto + 3 бесплатных (оба правила: ':free' и нулевые цены), сортировка
  по имени; все модели — платные с ценами за 1M ($0.15/$0.60,
  $1.14/$2.24, $3.00/$15.00) по возрастанию; выбор с галочкой и
  сохранением; восстановление выбора после перезагрузки из кэша (без
  сети); «Обновить список» — force-запрос; ошибка 500 → красный текст
  + «Повторить» → восстановление. Сборка без ошибок, тестовые данные
  убраны.
- Отправлено в GitHub (main).

## Что сделано (шаг 6)
- Генерация вопросов через ИИ: модуль OpenRouter (каскад моделей с
  таймаутом 60 сек, промпты, парсинг JSON с валидацией, нормализация
  и фильтр дублей, порции по 10); страница настроек с ключом
  (localStorage 'openrouter_api_key') и «Проверить ключ»; экран
  /questions/generate (тема, количество 1-50, мультивыбор сложностей,
  прогресс «Генерируем… N из M»); кнопка «✨ Сгенерировать через ИИ»
  на /questions; insert с source='ai'; тосты и редиректы по ошибкам.
  Найден и исправлен баг бесконечного цикла на порции из дублей.
  Подробности проверки — с тестовыми моками OpenRouter и Supabase:
  прогресс, снятие ```-обёртки, отбрасывание дубля и невалидного
  вопроса, каскад auto (500) → deepseek (200), сценарии без ключа и
  с неверным ключом. Сборка без ошибок, тестовые данные убраны.

## Что сделано (шаг 5)
- Создание вопроса: /questions/new — общий компонент QuestionForm
  (используется и редактированием), кнопка «+ Добавить вопрос» на
  /questions, insert (source = 'manual'), валидация, тосты, «Отмена».
  Проверено в браузере: форма, создание, валидация, ошибка сети.

## Что сделано (шаг 4.1)
- Фикс метки сложности: выводится всегда и первой (easy/medium/hard
  цветные; 'any' → «Любая сложность», NULL/пусто → «Без сложности» —
  серые). Видна в свёрнутой и развёрнутой карточке.

## Что сделано (шаг 4)
- Редактирование вопроса: /questions/:id/edit, форма с полями и
  валидацией, update в Supabase, тосты, «Вопрос не найден» →
  редирект, карандаш в карточке ведёт на форму.

## Что сделано (шаг 3)
- Экран «Банк вопросов»: список вопросов из Supabase, общая шапка,
  «← На главную» + «Всего: N вопросов», чипсы тем («Все темы», темы,
  «Без темы»), карточки с метками (сложность, «С подвохом», источник,
  тема) и раскрытием (варианты А–Г, правильный зелёный, ~200 мс),
  удаление с диалогом и toast, состояния загрузки/ошибки/пустоты.
  Адаптив 2 колонки / 1.

## Что сделано (шаг 2)
- Роутинг: /login, /home, /questions, /textbooks, /settings.
- Главный экран: шапка, приветствие, три карточки со счётчиками
  (questions/textbooks, «…» при загрузке, «—» при ошибке), «Выйти»,
  нижний блок про синхронизацию. Адаптив 3 колонки / 1.

## Что сделано (шаг 1)
- Экран входа: Email + Пароль, signInWithPassword, красные ошибки
  по-русски, редиректы, сессия сохраняется (localStorage).

## Что сделано (шаг 0)
- Установлены Node.js v24.21.0 LTS и Git 2.55.0; проект Vite + React 18
  + TypeScript; зависимости react-router-dom, @supabase/supabase-js,
  tailwindcss v3; VISION.md, PLAN.md, PROGRESS.md; Supabase-клиент;
  экран-заглушка; публикация в GitHub.

## Известные проблемы
- Проверка ключа Supabase через curl давала «Invalid API key», однако
  пользователь подтвердил, что ключ валиден; реальный запрос из
  браузера проходит. Игнорируем.
- Сборка предупреждает о размере бандла ~500 kB — не критично,
  можно ускорить код-сплиттингом на шаге 9 (полировка).
- OpenRouter вызывается прямо из браузера — у OpenRouter CORS
  разрешён; если при реальном ключе браузер заблокирует запрос,
  понадобится небольшой прокси (решим по факту проверки).
- Реальные данные и реальная генерация проверяются с настоящим
  ключом OpenRouter; вся механика проверена с тестовыми моками.

## Заметки
- Инструменты установлены в пользовательские папки:
  Node — %LOCALAPPDATA%\Programs\nodejs,
  Git (Portable) — %LOCALAPPDATA%\Programs\Git.
- Прокси для GitHub настроен в %USERPROFILE%\.gitconfig
  (http://127.0.0.1:10808).
- В системном конфиге PortableGit заменён credential-helper-selector
  на store (селектор показывал невидимый диалог и puши зависали);
  токен GitHub — в %USERPROFILE%\.git-credentials.
- Tailwind v3 (tailwind.config.js + @tailwind). Сессия Supabase —
  в localStorage (persistSession включён по умолчанию); ключ и модель
  OpenRouter — тоже в localStorage.
- Тестовые сессии/данные/ключи подкладывались только временно в
  браузере (перехватчик fetch) и удалены после проверки; в коде их нет.
