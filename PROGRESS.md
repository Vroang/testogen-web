# PROGRESS — веб-версия

## Текущий шаг: 2 — главный экран
## Статус: шаг 2 выполнен

## Что сделано (шаг 2)
- Роутинг: /login, /home (главный экран), /questions, /textbooks,
  /settings (заглушки «Скоро»). Все закрытые маршруты редиректят
  на /login без сессии; / ведёт на /home (если залогинен) или /login.
- Шапка: логотип (teal-квадрат «Т») + «ТестоГен» слева; имя
  пользователя (часть email до @, полный email в подсказке) и кнопка
  «Выйти» справа.
- Блок приветствия «Здравствуйте!» / «Выберите, что хотите сделать».
- Три карточки-ссылки (белые, скругления 16px, мягкая тень, при
  наведении тень усиливается и карточка приподнимается):
  «Банк вопросов» (📚, счётчик вопросов), «Мои учебники» (📖, счётчик
  учебников), «Настройки» (⚙️, «Аккаунт и синхронизация»).
- Счётчики: запросы count к questions/textbooks с фильтром по user_id
  (RLS поверх). Пока грузится — «…»; при ошибке — «—» и запись в
  console.error; экран не блокируется. Русские формы слов
  (1 вопрос / 2 вопроса / 5 вопросов, учебник/учебника/учебников).
- Нижний блок: «Все данные синхронизируются с мобильным приложением».
- «Выйти»: supabase.auth.signOut() → редирект на /login.
- Адаптивность: на широком экране 3 колонки, на узком — одна
  (проверено по геометрии сетки на 390px и 1280px).
- Фон #FAFAF7, акцент #0E7C6B — на новых экранах; LoginPage не менялся.
- Проверено в браузере: без сессии / и /home → /login; с подложенной
  тестовой сессией / → /home, весь экран отрисован; переход по карточке
  на /questions «Скоро» и назад; «Выйти» → /login, хранилище сессии
  очищено. Таблицы questions/textbooks и колонка user_id существуют
  (проверено запросом из браузера). Сборка без ошибок.
- Отправлено в GitHub (main).

## Что сделано (шаг 1)
- Экран входа (src/pages/LoginPage.tsx): поля Email и Пароль (скрыт
  точками), кнопка «Войти», логотип «Т» и заголовок «ТестоГен».
- Вход через Supabase Auth: signInWithPassword. При успехе сессия
  сохраняется SDK (localStorage) и срабатывает редирект на главный экран.
- При ошибке — красное сообщение под кнопкой, популярные ошибки
  переведены на русский («Неверный email или пароль» и др.).
- Проверка сессии при запуске (getSession) и слушатель onAuthStateChange:
  не залогинен → /login, залогинен → /. Работает и при перезагрузке.

## Что сделано (шаг 0)
- Установлены Node.js v24.21.0 LTS и Git 2.55.0 (в пользовательские
  папки, без прав администратора), добавлены в PATH.
- Создан проект Vite + React 18 + TypeScript.
- Зависимости: react-router-dom, @supabase/supabase-js,
  tailwindcss (v3) + postcss + autoprefixer.
- Настроен Tailwind; созданы VISION.md, PLAN.md, PROGRESS.md.
- Supabase-клиент: src/lib/supabase.ts; экран-заглушка; проверка сборки
  и dev-сервера; публикация в GitHub.

## Известные проблемы
- Проверка ключа Supabase через curl давала «Invalid API key», однако
  пользователь подтвердил, что ключ валиден, и реальный запрос из
  браузера (signInWithPassword) проходит: сервер отвечает ошибкой
  авторизации «Invalid login credentials» при неверном пароле — то есть
  ключ принят. Проблема была в способе проверки, игнорируем.
- Пункты чек-листа, требующие реального входа (реальные числа
  счётчиков, повторный вход, сохранение сессии после F5), проверяются
  с настоящими учётными данными — механика написана, частично
  проверена через подложенную тестовую сессию.

## Заметки
- Инструменты установлены в пользовательские папки:
  Node — %LOCALAPPDATA%\Programs\nodejs,
  Git (Portable) — %LOCALAPPDATA%\Programs\Git.
- Прокси для GitHub уже был настроен в %USERPROFILE%\.gitconfig
  (http://127.0.0.1:10808) — оставлен как есть.
- В системном конфиге PortableGit заменён credential-helper-selector
  на обычное хранилище (store): селектор показывал невидимый диалог
  и puши зависали. Токен GitHub перенесён из диспетчера учётных
  данных Windows в %USERPROFILE%\.git-credentials.
- Tailwind зафиксирован на v3: стабильная линия с tailwind.config.js
  и директивами @tailwind (v4 использует другой механизм настройки).
- Сессия Supabase хранится в localStorage браузера (persistSession
  включён по умолчанию) — после перезагрузки страницы вход сохраняется.
- Тестовая сессия для проверки интерфейса подкладывалась только
  временно в браузере и удалена после проверки; в коде её нет.
